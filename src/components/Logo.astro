---
export interface Props {
    isoColor?: boolean;
}

const {
    isoColor = true, // Por defecto es a color
} = Astro.props

const logoSrc = isoColor 
    ? '/isologo.svg' 
    : '/isologoBlanco.svg';
---

<div>
    <img
        src={logoSrc}
        class="ucasal-logo w-36"
        alt="UCASAL LOGO"
    />
</div>
